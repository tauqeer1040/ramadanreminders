{
  "name": "meowmin-cf",
  "private": true,
  "version": "0.1.0",
  "description": "Cloudflare Pages deployment for Meowmin (Flutter web + Astro landing + Express API as _worker.js)",
  "scripts": {
    "build:worker": "node scripts/build-worker.mjs",
    "prepare:public": "node scripts/prepare-public.mjs",
    "build": "npm run prepare:public && npm run build:worker",
    "dev": "npm run build && wrangler pages dev public",
    "deploy": "npm run build && wrangler pages deploy public --project-name meowmin"
  },
  "devDependencies": {
    "esbuild": "^0.25.0",
    "wrangler": "^4.118.0"
  }
}
